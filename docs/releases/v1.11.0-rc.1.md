# v1.11.0-rc.1 (2026-08-04,待 Eric 拍板 GA 时间)

> **Pre-release**:rc.1 给 AI-Brain 内部 dogfooding 试用,**不建议生产使用**
> **基础**:`4afb9db`(v1.10.0-rc.1 release prep,2026-08-01)→ `6cee045`(5 份前序 merge 后)→ 等 `a8c0e28` / `5a3d36a` 拍板 merge
> **Mission**:M-002(Phase 2 — general 模式骨架,**5/5 milestone done,4 merged + 1 ready,等 Eric 拍板**)

## Added

- **MS-001(general 模式模板抽离)**:`templates/general/.agent/` 6 子目录骨架 + 4 workflow contracts
  - 4 模式 workflow:`/memory recall` / `/memory distill` / `/agent discover` / `/agent invoke`
  - 2 skill(runtime-continuity copy + memory-curator 新)+ 1 sub-agent(memory-curator)
  - 1 domain(general-purpose)+ 1 system prompt + 1 config + README
  - 双语同步(zh + en 各 3 个:prompts + domains + config)
  - 11 文件 + 0 改动 v1.x 锁定文件 + 0 npm 依赖
  - 详情:commit `eac3d9a`(merged `518139f`)
- **MS-002(`cortex-agent memory` CLI + 3 类 schema)**:`lib/memory/` 5 文件子系统 + 5 test 文件
  - `cortex-agent memory recall` / `cortex-agent memory distill` 子命令
  - 3 类型:episodic + semantic(已 ship)+ procedural(推到 v1.12 per D-002-2)
  - 文件 IO(atomic .tmp + rename)+ draft lifecycle + commit/rollback + run journal
  - 72/72 tests pass in 3.4s
  - 详情:commit `1289702`(merged `a465f01`)
- **MS-003(`cortex-agent agent discover|invoke` CLI + Agent Registry)**:`lib/agents/` 5 文件子系统
  - 静态能力 registry(`.agent/agents/<agent_id>.json`,1 file per entry,per `agent.schema.json`)
  - `cortex-agent agent discover` / `cortex-agent agent invoke` 子命令(M-002 scope)
  - 内部 sole dispatcher:routes `discover|invoke` → M-002,`report|launch` → M-008(lib/commands.js 零修改)
  - D-002-3 边界:Agent Registry(静态能力) vs Coordination Registry(M-008 运行时)严格分离
  - `invoke` plan-only(MS-003 边界,real dispatch 等 M-003 mission 5 adapters + 1 MCP bridge)
  - 77/77 tests pass in 2.6s
  - 详情:commit `a8c0e28`(**ready,等 Eric 拍板 merge**)
- **MS-004(4 general workflow + E2E 矩阵)**:`bin/cli.js initModeGeneral` 扩展 + `tests/m002-e2e-matrix.test.js`
  - `init --mode general` 同时 copy `templates/general/.agent/`(6 子目录 + 4 workflow files)到 `<baseDest>/general/`
  - `lib/agents/registry.js` 鲁棒性增强(listAgents try-catch skip non-entry files,M-001 mixed-content dir 兼容)
  - 端到端 matrix 7 tests:init → memory distill → recall → agent discover → invoke → journal 全跑通
  - 163/163 total regression pass(MS-002 72 + MS-003 77 + MS-004 7 + dispatch 7)
  - 详情:commit `5a3d36a`(**ready,等 Eric 拍板 merge,顺序:MS-003 → MS-004**)
- **MS-005(RFC v0.4 同步 + release notes final)**:`docs/architecture/general-mode-design.md` §15 Phase 2 5 行翻 ✅ + 本 release notes final 化
  - 沿用 M-001 MS-005 二次 commit 模式(commit `1a4d458`),本任务填全部 5 placeholder 为真实 commit hash
  - 详情:commit `[本 commit 自身]`

## Fixed

- RFC §15 Phase 1 4 个待办保持 ✅(M-001 收口,v1.10.0-rc.1 final 化)
- RFC §15 Phase 2 5 个待办全部翻 ✅(M-002 MS-001/002/003/004/005 收口,本 PR)
- M-013.P0 C2 dispatch 7 个 pre-existing fail 修通(独立于 M-002,已 ship `f71197f` merged `1759597`)
- FAE-002 spec 阶段 8 章节 + 16 validation assertions publish(`b3d8f53` merged `105198e`,等 Eric 整体批准后启动 M-004 实施 5-6 周)

## Notes

- **general 模式 opt-in / 暂不推荐生产**(`§12 #2` 拍板);通过 `cortex-agent init --mode general` 显式选择,默认行为不变
- **shadow 双跑路径**:`templates/{zh,en}/` 老项目零变化,新 `init --mode general` 走 v2 schema(由 MS-004 E2E 矩阵覆盖)
- **零依赖**:`templates/general/` 抽离无 npm install,hand-rolled draft-07 验证器沿用 v1.x 风格
- **MS-005 placeholder 模式**:沿用 M-001 MS-005 二次 commit 模式(commit `1a4d458`),release notes final 化阶段替换所有 placeholder 为真实 commit hash
- **M-002 范围**:本阶段完成 general 模式骨架(5/5 milestone done),**不包含** cross-host 适配(留 v1.12+)和跨厂商 agent 联邦(留 v2.0)
- **M-002 → M-003 mission 移交**:M-002 提供了 4 模式 workflow contracts + Agent Registry(静态) + Memory CLI(episodic + semantic),M-003 mission 实施 5 adapters(Claude Code / Codex / Codey / Pi / MiniMax CLI)+ 1 MCP bridge 时 wire 真实 dispatch 到 M-002 的 plan 接口
- **MS-003 / MS-004 拍板顺序**:MS-003 必须先 merge(MS-004 base = MS-003 commit),Eric 周一拍板后主 session 串接

## Backward Compatibility

- `cortex-agent init --mode code` 行为与 v1.10.0-rc.1 完全一致
- 现有 v1.10.x 项目 `cortex-agent update` 升级零影响(shadow 路径)
- `update --mode general` 暂不允许跨模式升级(v2.0.0 引入 `cortex-agent migrate` 替代)
- 所有 v1.10.x 已有子命令(`init / update / upgrade / session / task / event / lease / secrets / query / dispatch / memory / agent / 等`)函数体零修改
  - M-002 新增 `memory` / `agent discover|invoke` 是纯加法子命令(per FAE-001 / M-013.P0 / MS-002 / MS-003 模式,direct require + case route)
  - M-008 协调 runtime 的 `agent report|launch` 走 `lib/commands.js`(M-001 既有,零修改)
  - M-002 dispatcher 内部 forward M-008 subcommand,边界清晰

## Known Limitations

- ❌ ~~`cortex-agent memory` CLI 暂未 ship~~ ✅ **shipped in MS-002**(`1289702` merged `a465f01`)
- ❌ ~~`cortex-agent agent discover|invoke` CLI 暂未 ship~~ ✅ **shipped in MS-003**(`a8c0e28` ready)
- ⚠️ **MS-003 / MS-004 拍板 merge 待 Eric 拍板**(8-04 周一上午);main HEAD 6cee045 仍不含 M-002 完整 5/5 milestone
- **procedural memory 仍推 v1.12**(M-002 MS-002 只 ship episodic + semantic per D-002-2)
- **5 adapters + 1 MCP bridge**(Claude Code / Codex / Codey / Pi / MiniMax CLI)**完整实施留 v1.12+ 或 v2.0**;本 rc 只 ship Agent Registry 静态 + invoke plan-only 框架,real dispatch 等 M-003 mission(6-7 周)
- **cross-host 适配**:host 主动调用 `cortex-agent session host-switch` 未做(留 v1.12+)
- **跨厂商 agent 联邦**:`agents/capabilities/` 标准化 capability 描述未 ship(留 v2.0)
- **FAE-002 framework 真事件总线**:`lib/event-bus/` 实施留 M-004 mission(5-6 周,等 Eric 拍 spec 整体批准)

## Upgrade Path

- v1.10.0-rc.1 → v1.11.0-rc.1:`npx cortex-agent update` 即可,零影响
- v1.11.0-rc.1 → v1.11.0 GA(待发):同样 `npx cortex-agent update`
- v1.11.0 → v2.0.0(未来):走 `cortex-agent migrate` 跨模式升级,提供 dry-run

## 决策项拍板记录(16 D-XXX,2026-08-01 选 A 全部默认)

| Mission | 决策项 | 拍板数 |
| :--- | :--- | :---: |
| M-002 | D-002-1/2/3/4/5/6 | 6 |
| FAE-002 | D-FAE-002-1/2/3/4/5 | 5 |
| M-013 | D-M013-1/2/3/4/5 | 5 |
| **总** | | **16** |

## References

- RFC: `docs/architecture/general-mode-design.md` (v0.3 §17 v2.0 愿景 + v0.4 §15 Phase 2 翻 ✅)
- M-001 mission: `.agent/missions/M-001/mission-plan.md` (Phase 1 收口)
- M-002 mission: `.agent/missions/M-002/mission-plan.md` (Phase 2,本 PR 收口 5/5)
- M-002 bridge memos (跨 session 决策归档):
  - `AI-Brain/99-Project-Notes/cortex-bridge/2026-08-01-dxxx-16-decisions-approved.md`
  - `AI-Brain/99-Project-Notes/cortex-bridge/2026-08-02-4-workers-done-2-takeover-and-monday-review-queue.md`
  - `AI-Brain/99-Project-Notes/cortex-bridge/2026-08-03-m002-ms002-memory-cli-shipped.md`
  - `AI-Brain/99-Project-Notes/cortex-bridge/2026-08-03-m002-ms003-agent-registry-cli-shipped.md`
  - `AI-Brain/99-Project-Notes/cortex-bridge/2026-08-03-m002-ms004-e2e-matrix-shipped.md` (本 PR 配套)
- MS-005 validation contract: `.agent/missions/M-002/validation-contract-ms-005.json`
- v1.10.0-rc.1 release notes: `docs/releases/v1.10.0-rc.1.md`
- CHANGELOG: `CHANGELOG.md`
- Memory + Agent CLI handoffs: `.agent/missions/M-002/handoffs/20260803-*.{md,json}`
