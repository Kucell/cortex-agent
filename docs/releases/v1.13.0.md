# v1.13.0 (2026-08-25, Cortex Agent — Token Control Plane + Codex-only Measurement Gate)

> **Stable release**: v1.13.0 由 `v1.13.0-rc.4` (`f82397c`) → 主分支 17 commits → `33b7186` (`chore(release): v1.13.0`)。
> **安装**: `npm i -g cortex-agent`（latest tag 从 `1.8.0` 切到 `1.13.0`）；已有项目再跑 `cortex-agent update` 同步模板。
> **Claim scope (重要)**: **Codex host only** — 21,563 receipts / 14 days / **7 consecutive days ≥100/天** (`8-06 ~ 8-14`)，满足 P-005 §5 single-host spirit；DSH/PI 跨 Host 验证留 Phase C Stage 2（不阻塞 stable 发布）。
>
> **决策**: `D-release-1.13.0-stable-36bacff` (用户拍板 path-A: Codex-only claim，2026-08-25T02:25Z)。
> **Waitpoint**: `WP-release-1.13.0-stable-36bacff` (released)。

## 中文版

### 重大特性 (Major Features)

#### 1. Token Control Plane (M-025) — Measurement Infra 完成
代码层完成 Phase B shadow measurement infrastructure，**主 ledger 累计 31,255 receipts**：

| Agent Host | Receipts | 覆盖 | 状态 |
| :--- | ---: | :--- | :--- |
| **Codex** | 21,563 | 14 天 / 7天连续 ≥100/天 | ✅ P-005 §5 Codex-only pass |
| **DSH** | 832 | 2 天（8-18 + 8-19） | ⏸ Phase C Stage 2 待数据回潮 |
| **Pi** | 8,858 | 16 天（含 8-18 + 8-19） | ✅ 已接入 |
| Test (excluded) | 2 | — | — |

- **DSH agent host 接入** (`scripts/dsh-usage-sync.js`)：扫描 DSH session.jsonl.zstd 提取 usage → MS-001 receipt → 主 ledger
- **Pi agent host 接入** (`scripts/pi-usage-sync.js`, M-025/MS-006, D-M025-MS006-pi-bridge approved 2026-08-19T05:25Z)：扫描 `~/.pi/agent/sessions/<slug>/*.jsonl` 的 `type:"message"` 行 public usage → MS-001 receipt；8858 receipts backfill，**statistics not restarted**（Phase B 窗口起点保持 8-18）
- **3 端 usage-sync 测试化**：opencodex (4/4) + dsh (11/11, zstd→node zlib) + pi (8/8)
- **DSH script 可移植性化**：移除 `zstd` 外部 CLI 依赖，改用 Node 内置 `zlib.createZstdDecompress`（Node 22+ 标准 API）；9 个 fixture 测试从 ERROR → PASS

#### 2. Phase C Stage 1 Pre-flight — 7/7 ALL GREEN（真实执行）

`scripts/phase-c-stage1-preflight.sh` 现已完成 7 步测量门禁：

| Gate | Status |
| :--- | :--- |
| sample_gate (agent-host dimension) | ✅ PASS（agent_hosts=codex,dsh,pi + eligible=31,253） |
| inference_rate (≤5%) | ✅ PASS (0%) |
| reproducibility (sha256 lock) | ✅ PASS |
| tests (≥90% pass) | ✅ PASS (287/287) |
| arch_guard | ✅ PASS |
| active_audit | ✅ PASS |
| **rollback_drill (real execution)** | ✅ **PASS**（新增 `runtime-state-layout-migration.js` + 5/5 测试） |

**rollback_drill 真实执行**——此前一直是 `SKIPPED`，本次新增 `templates/_shared/.agent/skills/management-api/scripts/runtime-state-layout-migration.js`（rollback/forward 双模式 + contract orphan 检测）+ preflight MIG_SCRIPT 路径修正，**首次真实 PASS**。

#### 3. Pilot 矩阵 4/4 全部升级到 v1.13.0（rc.10/rc.13/rc.14）

| Pilot 项目 | Cortex-Agent 版本 | KI-3 resolved | Artifact |
| :--- | :--- | :--- | :--- |
| SamHMI | rc.13 → **rc.14** | 2026-08-19T02:55Z | `SamHMI.json` |
| hmi-platform | rc.13 → **rc.14** | 2026-08-19T02:55Z | `hmi-platform.json` |
| csm-view | rc.13 → **rc.14** | 2026-08-19T02:55Z | `csm-view-memory-rc10.json` |
| samkoonyun-mobile | rc.10 → **rc.14** | 2026-08-19T05:30Z | `samkoonyun-mobile.json` |

#### 5. v1.13.0-rc.4 已发布功能（继承自 rc.4）

- **proposal-share 提案包共享工作流** (`T-H14`)：双仓联合提案 export → verify → import
- **.claude-plugin 同步**：plugin.json + marketplace.json 与 package.json 版本号三处对齐

### 验证 (Validation)

- **测试基线**: 287/287 PASS
  - scripts scope: **5/5** (token-savings-demo + pi-usage-sync + dsh-usage-sync + opencodex-usage-sync + runtime-state-layout-migration)
  - shadow-usage (host-adapter): **71/71**
  - context-budget v2: **67/67**
- **Preflight**: 7/7 ALL GREEN（含真实 rollback_drill）
- **本地发布链路**: `npm pack` → 1077 files / 1.7MB packed / 6.5MB unpacked / sha256 锁定
- **4/4 Pilot 项目**: upgrade + update + verify 全部通过 + KI-3 orphan schema 全部清理

### 已知事项 (Notes)

| ID | Description | Resolution |
| :--- | :--- | :--- |
| **KI-1** | runtime-state-layout rollback drill | ✅ **已 resolved**（真实执行 PASS） |
| **KI-2** | 模板源 SKILL.md 硬编码绝对路径 | 下个 release cycle templating |
| **KI-3** | orphan schemas (deprecated after runtime-state-layout 切换) | ✅ **已 resolved**（4 个 Pilot 全部清理） |
| **Codex-only claim** | DSH/PI 跨 Host 验证 | Phase C Stage 2（后续 RC 周期） |

### 发布执行 (Release Execution)

| Step | Status |
| :--- | :--- |
| 1. 版本号推断 (Conventional Commits: 5 feat → minor → 1.13.0) | ✅ |
| 2. 三处版本号同步 (package.json + plugin.json + marketplace.json) | ✅ |
| 3. `git commit chore(release): v1.13.0` + `git tag -a v1.13.0` | ✅ HEAD=`33b7186` |
| 4. `git push origin main` + `git push origin v1.13.0` | ✅ origin/main=`33b7186` |
| 5. **npm publish** (待用户拍板执行) | ⏳ pending user approval |

### 历史发版链

- `v1.13.0-rc.1` (2026-08-06) — 设计验证
- `v1.13.0-rc.2` (2026-08-07) — 设计验证
- `v1.13.0-rc.3` (2026-08-19) — **runtime-state-layout 收口 + TCP 第三 Host (未 draft)**
- `v1.13.0-rc.4` (2026-08-20) — **proposal-share 工作流 (已 publish 到 npm dist-tags.next)**
- **`v1.13.0` (2026-08-25)** — **Token Control Plane M-025 + Codex-only Measurement Gate (stable)**

---

## English Summary

**Major**: Token Control Plane measurement infra (M-025) ships with **31,255 receipts** across 3 agent hosts (Codex 21,563 / DSH 832 / Pi 8,858). Codex host alone satisfies P-005 §5 spirit (21,563 receipts / 14 days / 7 consecutive days ≥100/天). Phase C Stage 1 preflight is **7/7 ALL GREEN** with real `rollback_drill` execution. 4/4 Pilot projects upgraded to rc.14 with KI-3 orphan schemas cleaned. 287/287 tests pass.

**Breaking changes**: None.
**Migration path**: `npm i -g cortex-agent@latest` (from 1.8.0) or upgrade in-place via `cortex-agent upgrade + update`.

**Decision**: `D-release-1.13.0-stable-36bacff` (path-A: Codex-only claim, user-approved 2026-08-25T02:25Z). DSH/PI cross-host validation deferred to Phase C Stage 2 (next RC cycle, gated on `agent-runtime-interoperability` P-007/M-031 completion).