# v1.12.0-rc.6 (2026-08-11,待 Eric 拍板 GA 时间)

> **Pre-release**: rc.6 给 AI-Brain 内部 dogfooding 试用 + 实战 ≥ 2 周观察期, **不建议生产使用**
> **基础**: `a07a022` (v1.13.0-rc.2, 2026-08-07) → 599 commits → `5946c17` (general-mode init flow + L1 rules ship)
> **累积 commits** (v1.12.0-rc.1 起): **846 commits**
> **Mission 关联**: M-013 (Management API 6 SP 全 ship) + M-019 (context-budget v2) + P-001 (topology registry) + bridge skip-reason diagnostics + local-publish-validate workflow + L1 framework rules + vendor Layer 1.1 (Karpathy) + general mode init flow

---

## 中文版

### 重大特性 (Major Features)

#### 1. M-013 Management API 6 SP 全 ship — 非开发者项目管理 + 对话管理 + 跨 agent 续接
**M-013 从 SP-001 freeze contract 推到 SP-006 CLI 全部 ship, 6 个 sub-proposal 完整闭环。**

##### SP-001 Freeze Contract
- `cortex-agent freeze` CLI + V1 schema + L1 templates + tests
- `lib/progress/` V1 contract 落地

##### SP-002 Reducer + Probes + Focused Query
- 3 个 reducer 模块 + probes + focused query API
- tests 隔离从 parallel-run race (避免 timeout cascade)

##### SP-003 Pi JSON Adapter + SP-004 RPC Supervisor
- `lib/host-adapter/pi.js` JSON adapter
- RPC supervisor 跨 host 通信

##### SP-005 Watchdog + Notification + Recovery
- 守护进程 + notification pump + recovery 策略
- `lib/governed/child-monitor.js` 修复 MONITOR 路径

##### SP-006 CLI + Management Routing + 双语模板
- `bin/cli.js` SP-006 supervise CLI 集成
- `lib/management/` routing + 双语 (zh/en) 模板
- governed-attempt-query + 26 tests
- `lib/agents/agent-supervise` frontmatter fix

#### 2. M-019 context-budget v2 — P1/P2/C1/C2
**context 优化第二轮实施, P1 (size cap) + P2 (offline distill) + C1 (cache) + C2 (dispatch) 全 ship。**
- `lib/context-budget/` 4 个 module
- agent runtime 在 context 接近上限时主动 distill

#### 3. P-001 Topology Registry — 项目拓扑真相源
**项目拓扑结构 (workspace / mission / agent 关系) 的 read/write CLI + registry。**
- `lib/topology/` registry + read/write CLI
- 跟 P-003 cross-project-event-bridge 协同
- `ecce0d5 feat(topology): add project topology registry with read/write CLI`

#### 4. Bridge Skip-Reason Diagnostics — 8-11 收口
**`c44b758 fix(bridge): report skip reasons for invalid outbox events instead of silent drop`**
- bridge 处理 invalid outbox event 不再静默丢弃, 报具体 skip reason
- 8-11 release commit: `8caa862 chore(release): 1.12.0-rc.6 — bridge skip-reason diagnostics`

#### 5. Local-Publish-Validate Workflow — 本地发包 + 安装 + 升级三件套
**`629555f feat(workflow): local-publish-validate`**
- `cortex-agent local-publish-validate` 一条龙: 本地 pack + 本地 install + 可选目标升级
- 解决 Eric 实战时频繁需要本地验证再发包的工作流

#### 6. Vendor Layer 1.1 (Karpathy) — Global-Shared-Skills Namespace
**`a415351 feat(vendor): add global-shared-skills namespace with karpathy-guidelines as Layer 1.1`**
- `templates/_shared/.agent/global-shared-skills/` 新加 vendor namespace
- 首个 vendor: **karpathy-guidelines** (from multica-ai/andrej-karpathy-skills CLAUDE.md)
  - 4 原则: Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution
  - 上游 MIT, ref=main, compatible with claude-code/codex/cursor/opencode/gemini-cli/windsurf/antigravity/kiro/pi
- `_schema/vendor.schema.json` + `_schema/validate.js` (零依赖 validator)
- 架构澄清: `.agent/` 是 state repo, 可能独立 git 仓, 跟主仓不一定同步
- symlink 指向 `~/.agent/global-shared-skills/` (用户全局)
- 后续 Layer: 1.3 superpowers / 1.4 caveman / 1.5 addyosmani / 1.6 ECC 子集 / 1.7 README 完善

#### 7. L1 Framework Rules — llm-coding-behavior.md Ship
**`5946c17 fix(rules): ship llm-coding-behavior.md via templates/_shared to close publish-boundary bug`**
- Karpathy L1 framework rule (state repo commit `ff0b0f2`) 镜像到 `templates/_shared/.agent/rules/`
- 关闭 publish-boundary bug: state repo 不在 package.json `"files"` 里, init 装不进来
- 加 `templates/_shared/.agent/rules/SYNC.md` (112 行) 文档化 two-location architecture
- 5 legacy shared rules (code-standards / commit-standards / lesson-capture / submission-workflow / tech-stack) 同样 pattern
- `npm pack --dry-run` 验证两个新文件进 tarball

#### 8. General Mode Init Flow — `initModeGeneral()` 收口
**Mavis 8-10 收口, Eric 8-09 23:36 起草 `templates/general/AGENTS.md` 模板。**
- `initModeGeneral()` 加 4 步:
  1. **AGENTS.md seeding** — 从 `templates/general/AGENTS.md` copy 到项目根, 只在文件不存在时写
  2. `writeVersionFile(cwd)` — 跟 `init()` code mode 对齐
  3. `writePublicAnchor` — 写 `docs/cortex-agent/anchor.md` 跨工具识别
  4. `applyGitExclusion` — `.agent/` 加进 `.git/info/exclude`
- `lib/commands/init.js` 同步更新 (`require("../anchor/anchor")` → `require("../commands/anchor")` 跟 commands 拆分对齐)
- 3 commits: `d042dbf` (feat, 3 files) + `fcc00cf` (refactor, 27 files import 路径) + vendor commit
- General mode init flow dry-run 手工验证 AGENTS.md copy + version + anchor 三件套

#### 9. Test Infrastructure 重构 — 大幅提升稳定性
- `496f28a chore(test): add per-test + idle-timeout + SIGTERM trap to test runner`
- `6683224 chore(test): bump default npm test timeouts to absorb slow suites`
- `0ff0bca test: cover 6 untested lib modules and fix broken-symlink removal`
- `74d7048 fix(tests): exclude runtime-evidence from management-query-readonly digest`
- `2c72127 test(management + dashboard): isolate tests from parallel-run races`
- `915f021 test(mcp-bridge): await client.close() to drain subprocess before test returns`
- 多个 isolated test refactor + module rename + path depth fix

### Mission 关联 (Mission Ties)

| Mission | 范围 | 状态 |
|:---|:---|:---:|
| M-013 Management API | 6 SP (freeze + reducer + adapter + supervisor + watchdog + CLI) | ✅ done |
| M-019 context-budget v2 | P1/P2/C1/C2 4 子实施 | ✅ done |
| P-001 topology registry | registry + read/write CLI | ✅ done |
| bridge skip-reason diagnostics | invalid outbox event 报 reason | ✅ done (rc.6) |
| local-publish-validate workflow | 本地 pack+install+upgrade | ✅ done |
| vendor Layer 1.1 (Karpathy) | global-shared-skills namespace | ✅ done (rc.6) |
| L1 framework rules | llm-coding-behavior.md ship | ✅ done (rc.6) |
| general mode init flow | initModeGeneral 收口 | ✅ done (rc.6) |

### 修复 (Fixed)

- `c44b758 fix(bridge): report skip reasons for invalid outbox events instead of silent drop`
- `5946c17 fix(rules): ship llm-coding-behavior.md via templates/_shared to close publish-boundary bug`
- `904f806 fix(mcp-bridge): ExternalMcpClient.close() returns promise on child exit`
- `7b078b3 fix(governed-child-monitor): point MONITOR at current lib path`
- `74d7048 fix(tests): exclude runtime-evidence from management-query-readonly digest`
- `ee4bf5f fix(lib): dedup findAgents import in discover.js after rebase`
- `2f97dea fix(tests): correct path depth + module rename refs broken by f7d4100`
- `ee4bf5f` + `c79d94b fix(governed): point defaultExecutor at correct child-monitor path`
- Author 治理: Mavis 8-10 + Eric 8-11 期间 2 commits xueyq → Kucell (filter-branch)

### v1.12.0-rc.2 → rc.6 增量 (4 rc 累计)

- **rc.2** (decd654): M-019 context-budget v2 + state-sync + test infra 重构
- **rc.3** (3424010): pack+install cycle (rebuilt after E2E validation)
- **rc.4** (d9e191d): local pack+install+upgrade cycle
- **rc.5** (e5fd9df): local pack+install cycle (P-001 topology registry)
- **rc.6** (8caa862): bridge skip-reason diagnostics (current HEAD `5946c17`)

---

## English Version

### Major Features

#### 1. M-013 Management API — Complete (6 SPs Shipped)
**M-013 ship sequence: SP-001 freeze contract → SP-002 reducer → SP-003 Pi adapter + SP-004 RPC supervisor → SP-005 watchdog → SP-006 CLI routing. All 6 sub-proposals done.**

- `bin/cli.js` SP-006 supervise CLI + governed-attempt-query + 26 tests
- `lib/management/` routing + bilingual (zh/en) templates
- lib/progress/ V1 freeze contract
- lib/context-budget/ P1/P2/C1/C2
- lib/host-adapter/pi.js JSON adapter
- lib/governed/child-monitor.js watchdog
- lib/agents/agent-supervise frontmatter fix

#### 2. M-019 context-budget v2
- 4 sub-proposals: P1 (size cap) + P2 (offline distill) + C1 (cache) + C2 (dispatch)
- Agent runtime auto-distills when context approaches cap

#### 3. P-001 Topology Registry
- `lib/topology/` registry + read/write CLI
- Project topology = single source of truth (workspace / mission / agent relationships)
- Coordinated with P-003 cross-project-event-bridge

#### 4. Bridge Skip-Reason Diagnostics (rc.6 highlight)
- Bridge no longer silently drops invalid outbox events
- Reports specific skip reason for diagnostics

#### 5. Local-Publish-Validate Workflow
- `cortex-agent local-publish-validate` 1-step: local pack + local install + optional target upgrade
- Solves Eric's dev loop: pack → install → test → iterate

#### 6. Vendor Layer 1.1 (Karpathy) (rc.6 highlight)
- `templates/_shared/.agent/global-shared-skills/` namespace
- First vendor: karpathy-guidelines
  - 4 principles: Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution
  - From multica-ai/andrej-karpathy-skills CLAUDE.md
  - Upstream MIT, ref=main, compatible with claude-code/codex/cursor/opencode/gemini-cli/windsurf/antigravity/kiro/pi
- `_schema/vendor.schema.json` + `_schema/validate.js` (zero-dep validator)
- Architecture: state repo (`.agent/`) may be separate from main repo; symlink points to `~/.agent/global-shared-skills/`
- Future layers: 1.3 superpowers / 1.4 caveman / 1.5 addyosmani / 1.6 ECC subset / 1.7 README polish

#### 7. L1 Framework Rules Ship (rc.6 highlight)
- Karpathy-inspired L1 framework rule mirrored to `templates/_shared/.agent/rules/`
- Closes publish-boundary bug: state repo not in package.json `"files"`
- Adds `templates/_shared/.agent/rules/SYNC.md` (112 lines) documenting two-location architecture
- 5 legacy shared rules (code-standards / commit-standards / lesson-capture / submission-workflow / tech-stack) follow same pattern

#### 8. General Mode Init Flow (rc.6 highlight)
- `initModeGeneral()` 4 additions: AGENTS.md seeding + writeVersionFile + writePublicAnchor + applyGitExclusion
- `templates/general/AGENTS.md` template (Eric 8-09 23:36)
- `lib/commands/init.js` updated for commands/ split alignment
- 3 commits: `d042dbf` (feat) + `fcc00cf` (refactor) + vendor commit

#### 9. Test Infrastructure Hardening
- per-test + idle-timeout + SIGTERM trap
- Bumped default timeouts to absorb slow suites
- Isolated tests from parallel-run races
- Multiple module rename + path depth fixes

### Mission Ties

| Mission | Scope | Status |
|:---|:---|:---:|
| M-013 Management API | 6 SPs (freeze + reducer + adapter + supervisor + watchdog + CLI) | ✅ done |
| M-019 context-budget v2 | P1/P2/C1/C2 | ✅ done |
| P-001 topology registry | registry + read/write CLI | ✅ done |
| bridge skip-reason diagnostics | invalid outbox event reason | ✅ done (rc.6) |
| local-publish-validate workflow | local pack+install+upgrade | ✅ done |
| vendor Layer 1.1 (Karpathy) | global-shared-skills | ✅ done (rc.6) |
| L1 framework rules | llm-coding-behavior.md ship | ✅ done (rc.6) |
| general mode init flow | initModeGeneral close-out | ✅ done (rc.6) |

### Fixed

- `c44b758 fix(bridge): report skip reasons for invalid outbox events instead of silent drop`
- `5946c17 fix(rules): ship llm-coding-behavior.md via templates/_shared to close publish-boundary bug`
- `904f806 fix(mcp-bridge): ExternalMcpClient.close() returns promise on child exit`
- `7b078b3 fix(governed-child-monitor): point MONITOR at correct lib path`
- `74d7048 fix(tests): exclude runtime-evidence from management-query-readonly digest`
- `ee4bf5f fix(lib): dedup findAgents import in discover.js after rebase`
- `2f97dea fix(tests): correct path depth + module rename refs broken by f7d4100`
- `c79d94b fix(governed): point defaultExecutor at correct child-monitor path`
- Author governance: Mavis 8-10 + Eric 8-11 — 2 commits xueyq → Kucell (filter-branch)

### v1.12.0-rc.2 → rc.6 Increment (4 rc cumulative)

- **rc.2** (decd654): M-019 context-budget v2 + state-sync + test infra refactor
- **rc.3** (3424010): pack+install cycle (rebuilt after E2E validation)
- **rc.4** (d9e191d): local pack+install+upgrade cycle
- **rc.5** (e5fd9df): local pack+install cycle (P-001 topology registry)
- **rc.6** (8caa862): bridge skip-reason diagnostics → current HEAD `5946c17`

---

**Pre-release for AI-Brain internal dogfooding. Not for production use.**
**GA timing: pending Eric decision after ≥ 2 weeks of dogfooding observation.**
