# 执行计划资产

本目录用于保存跨会话、可追踪的执行计划。

`.agent/plans/` 仍然负责执行期进度管理；
`docs/exec-plans/` 负责保存更稳定、可归档、可回看的计划资产。

## 生命周期

- `active/`：当前正在推进的计划
- `completed/`：已完成并归档的计划

## 使用规则

- 复杂任务在 `/arch-design` 或 `/plan` 后，应在 `active/` 下落一份可持续追踪的计划
- 任务完成后，将计划迁移到 `completed/` 或标记为完成
- 计划中应包含目标、范围、任务拆解、验收标准和下一步
