# AI Debug Artifacts

Use this directory for temporary artifacts produced during AI-assisted debugging.

```text
.agent/debug/
├── screenshots/
├── logs/
└── temp/
```

Keep long-lived release evidence under `.agent/missions/<id>/evidence/` or project documentation assets instead.
