# Runtime

Project-level portable runtime data lives under `.agent/runtime/` in a project after `cortex-agent update` activates the runtime-state-layout marker. New schema files belong under `.agent/contracts/runtime-state/`, not here.
