# Milestone: MS-002

## Goal

在不访问 Host 私有 transcript/storage、也不改变执行策略的前提下，让两个不同的受管 Host adapter 以 shadow 模式产生并持久化 MS-001 schema-valid token-attempt receipt。

## Scope

- Feature(s): F-005
- Baseline: outer commit `8942631`
- Worktree: `/Users/xueyq/myworks/cortex-agent-worktrees/T-TCP-MS002-PI`
- Branch: `agent/T-TCP-MS002-PI-shadow-hosts`
- Validation contract: `../validation-contract.json` VC-011～VC-015

## Ownership Scope

- `lib/host-adapter/` 中与 usage shadow collection 直接相关的新模块或最小扩展
- `lib/runtime-adapters/` 中 Host usage envelope 的纯映射（如确有必要）
- `tests/host-adapter/` 与 `tests/runtime-adapters/` 中 MS-002 focused tests
- MS-001 Management API template receipt/ledger helper的最小接线；不得改变其 schema 或安全边界

## Forbidden Scope

- 不读取或持久化 prompt、response、message/thinking body、tool args/result、source body、凭据或 Host 私有绝对路径
- 不修改模型选择、上下文选择/压缩、fan-out、timeout、stop、retry policy 或 launcher 命令参数
- 不实现 P-002～P-004，不发布节省比例，不启动 7 天基线结论
- 不修改共享 `.agent/` 治理文件，不推送、合并、创建 PR、发布或清理其他 worktree

## Status

- State: PASS
- Started: 2026-08-14
- Completed: 2026-08-14
- Coordination Task: `T-TCP-MS002-PI`
- Correlation: `M-025-MS-002`
- Implementation lease: `LEASE-1` (owner `pi-M025-MS002`, fencing token `1`, released normally)
- Validation/repair lease: `LEASE-2` (owner `root`, fencing token `2`, released normally)
- Governed launch: accepted at `2026-08-14T02:06:00.903Z`
- Pi handoff: `READY_FOR_REVIEW` at `2026-08-14T02:12:11.415Z`
- Validator: Root independently validated and repaired the uncommitted worktree
- Coordination result: Task moved to `COMPLETED` by `CE-M025-MS002-ROOT-PASS`
- Implementation commit: outer `1f04b164aa615c05e616756e551230e3f183a1a1` on `agent/T-TCP-MS002-PI-shadow-hosts`
- Main integration: merge commit `b3f27ea` from approved Decision `D-M025-integrate-MS002-f147b865`
- Main validation: Architecture Guard PASS and focused/regression suite `163/163` PASS

## Validation Assertions

| Assertion ID | Blocking | Required evidence |
| :--- | :--- | :--- |
| VC-011 | true | 两个不同 Host adapter 的真实受管 shadow invocation 各产生 schema-valid receipt |
| VC-012 | true | persistence inspection 仅含 allowlist 字段、计数、状态、digest 和 reason code |
| VC-013 | true | capability fixtures 证明 unsupported 字段保持 unknown/unavailable |
| VC-014 | true | duplicate/retry/restart replay 不重复计数 |
| VC-015 | true | diff/config review 证明 adapter 不能改变 execution policy |

## Validation Result

| Assertion ID | Result | Evidence |
| :--- | :--- | :--- |
| VC-011 | PASS | Pi 0.82.1 `turn_end` 与 Codex 0.146.0 `turn.completed` 的公开数字 usage envelope 各生成一条 schema/security-valid receipt，并在临时 MS-001 ledger 中持久化为 2 条；Pi 显式零值保持为零，Codex 探针报告 input `20076`、output `5` |
| VC-012 | PASS | recursive allowlist、敏感 key/pattern/private-path fail-closed；normalized result 与 receipt 均不保留 raw payload |
| VC-013 | PASS | Pi `totalTokens`、Codex `reasoning_output_tokens` 仅标为 unsupported，不参与拆分或推断；缺失字段保持 `unknown` |
| VC-014 | PASS | 重试/进程重建生成稳定 receipt ID；ledger duplicate replay 不重复计数 |
| VC-015 | PASS | 实现仅含纯映射、校验、digest 与 receipt 构造；无 subprocess、Host 私有存储或 execution-policy primitive |

Additional evidence:

- Focused + MS-001 regression + updater suite: `163/163` PASS.
- Normalizer suite: `22/22` PASS.
- Architecture Guard and `git diff --check`: PASS.
- Worktree implementation committed locally; no push, merge, PR or release occurred.
- Approved local merge completed on main; no push, PR or release occurred.

## Exit Gate

Pi 只能在 scoped tests 通过后上报 `READY_FOR_REVIEW`；该状态不是 PASS。Root 必须独立审查 diff、执行 VC-011～VC-015，并确认真实 receipt 边界后才可更新 Mission。
