# Milestone: MS-005

## Goal

实现 P-002 / P-003 / P-004 的代码与聚焦测试，使 Token Control Plane 具备：minimal|standard|deep 三层上下文投影、capability-aware 缓存/压缩策略、reserve/commit/release 预算账本。代码与中文/英文模板同步，不破坏 MS-001/002/003 的合約与安全闸门。

## Scope

- Feature(s): F-002 / F-003 / F-004
- Skill: `context-budget` (新增)
- Scripts:
  - `minimal-context.js` (P-002): minimal|standard|deep 三层投影 + token budget 截断
  - `context-traverse.js` (P-002): BFS/DFS bounded traversal
  - `context-expand.js` (P-002): minimal|standard|deep incremental expansion
  - `capability-detect.js` (P-003): supports_compaction / supports_prompt_cache / supports_render_receipt 等
  - `budget-ledger.js` (P-004): reserve / commit / release / expire，含 attempt_id 幂等 + oversell 保护
- Tests: `tests/context-budget/*.test.js` (67 cases)
- Templates sync: `templates/zh/.agent/skills/context-budget/scripts/` + `templates/en/.agent/skills/context-budget/scripts/` (byte-identical)

## Status

- State: PASS (implementation merged to main `982935c` + `2858099`)
- Started: 2026-08-14T21:02Z (P-002 minimal-context entry)
- Completed: 2026-08-14T21:13Z (P-002/P-003/P-004 full implementation merged)
- Validator: Root independent validation — 67 tests PASS (focused context-budget suite), shadow-usage 53/53 + capture-usage 5/5 = 58/58 still PASS
- Phase C prerequisite: implementation 已落 main，但 activation (P-002/P-003/P-004 对 governed agent runtime 生效) 仍需独立 resource-bound Decision；不在 MS-005 scope

## Validation Results

| Assertion ID | Type | Blocking | Status | Evidence |
| :--- | :--- | :--- | :--- | :--- |
| MS-005-01 | focused tests | true | PASS | `node --test tests/context-budget/*.test.js` → 67/67 PASS |
| MS-005-02 | templates parity | true | PASS | `templates/zh/.agent/skills/context-budget/scripts/` 与 `templates/en/.agent/skills/context-budget/scripts/` 双向 byte-identical |
| MS-005-03 | shadow-usage regression | true | PASS | `node --test tests/host-adapter/shadow-usage/*.test.js` → 53/53 + 5/5 = 58/58 PASS |
| MS-005-04 | Architecture Guard | true | PASS | Audit 1100+ source files; no violations |
| MS-005-05 | zero external deps | true | PASS | context-budget scripts 仅用 `node:fs/path/crypto` 等内置模块 |
| MS-005-06 | forbidden patterns | true | PASS | no `child_process` / `spawn` / `exec` / `\.pi/` / `\.claude/` / `\.codex/` / `context_compact|select` / `fan_out` / `stop_behavior` |

## Execution Plan (回顾)

### Phase 1: P-002 minimal context scaffold (commit 2858099)

- 新增 minimal-context.js: minimal|standard|deep 三层投影 + token budget 截断
- 复用 select.js 的 scoreModule/extractTaskTokens
- 输出 truncated/omitted/next_query
- 双语模板 zh/en 同步

### Phase 2: P-002 traverse/expand + P-003 capability-detect + P-004 budget-ledger (commit 982935c)

- context-traverse.js: BFS/DFS bounded traversal
- context-expand.js: minimal|standard|deep incremental expansion
- capability-detect.js: P-003 §1 contract (supports_compaction / supports_prompt_cache / supports_render_receipt)
- budget-ledger.js: reserve / commit / release / expire，attempt_id 幂等 + oversell 保护
- 67 tests cover all new scripts
- 双语模板同步 (byte-identical)

## Integration Path

- **不自动激活**：P-002/P-003/P-004 proposal 状态已更新为 `approved-implementation`；但对 governed agent runtime 实际生效仍需：
  1. Phase B 7-day observation window 触发 (双 Host 同 UTC day 首次 eligible receipt)
  2. Phase C 评估 P-005 evaluation rollout gates
  3. 独立 resource-bound Decision 才能让 P-002/P-003/P-004 真正改为 production traffic
- **back-compat**: MS-001 ledger + MS-002 host adapter 不变; P-005 evaluation gates 仍为唯一 activation 入口

## Decision

- Result: PASS (implementation merged; activation pending)
- Next state: await Phase B organic traffic + Phase C evaluation gates before P-002/P-003/P-004 production traffic
- handoff artifact: `.agent/artifacts/T-TCP-MS005-PI/001-handoff.json`
