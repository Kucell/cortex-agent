# Milestone: MS-001 — DSH capability snapshot + discover/health/report

> **Goal**: 实现 `DshAdapter` 的 `discover()` / `health()` / `report()` 与 `host-runtime-snapshots.js` 的 DSH capability snapshot 行；不连 dispatch、不动 registry、不改 `_seed()`；shadow usage 路径完全不变。
>
> **Feature**: F-014
> **Validation Contract**: `../validation-contract.json#MS-001`
> **对应项目里程碑**: `agent-runtime-interoperability/index.md` M-014

## Scope

- Feature(s): F-014
- Files/modules:
  - `lib/agents/adapters/dsh.js` (new, ~600 行)
  - `lib/agents/adapters/base.js` (zero modify)
  - `lib/runtime-adapters/host-runtime-snapshots.js` (新增 dsh snapshot)
  - `tests/agent/agent-adapter-dsh.test.js` (new, MS-001 切片)
- Validation contract: `../validation-contract.json#MS-001`

## Status

- State: PASS（EXECUTE_FEATURE 完成，VALIDATE_MILESTONE 由 mission-coordinator 接受）
- Started: 2026-08-19
- Completed: 2026-08-19
- Commit: `7d877a85f6bf18ccebbfc111485f0ebdd9172537` — `feat(agents): 加入 DSH 一类派发适配器 (MS-001 切片)`
- Validator: mission-coordinator + 独立 validator

## Validation Results

| Assertion ID | Type | Blocking | Status | Evidence |
| :--- | :--- | :--- | :--- | :--- |
| VC-029-001-01 | api | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` discover unit（`adapter_type==='dsh'`，capabilities 非空，`maturity==='stable'`，`host==='deepseek-harness'`，`receipt_contract==='ms-001'`） |
| VC-029-001-02 | test | true | **PASS** | `node --test tests/agent/agent-adapter-dsh.test.js` — 10/10 PASS（含 health ready=true via `which`、health() missing binary returns ready=false with structured error、shell:false + PATH-resolvable bin works） |
| VC-029-001-03 | api | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` capability descriptor test — `validateCapabilityDescriptor` from `lib/runtime-adapters/capability-contract.js` accepts all 7 P-001 frozen capability entries；descriptor 与 entry 全部 `Object.isFrozen` |
| VC-029-001-04 | security | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` security boundary test — strip block + line comments 后源码不含 `.dsh/sessions` 或 `session.jsonl` 引用；shadow usage 边界完全在 `scripts/dsh-usage-sync.js` |

## Command Evidence

| Command | Exit Code | Result |
| :--- | :--- | :--- |
| `node --test tests/agent/agent-adapter-dsh.test.js` | 0 | 10/10 PASS（discover、env override、capability descriptor validation + freeze、health ready via which、health with shell:false + PATH bin、health ready=false for missing bin、report() not_found、zero npm deps、security boundary） |
| `node --test tests/agent/*.test.js` | 0 | 458/458 PASS（既有 448 baseline + 10 新增；无回归） |
| `node --test tests/host-adapter/shadow-usage/*.test.js tests/scripts/dsh-usage-sync.test.js` | 0 | 82/82 PASS（shadow usage baseline 24/24 + 其他 shadow-usage 58/58 不退化） |
| `node .agent/skills/architecture-guard/scripts/index.js` | 0 | ✅ No architectural violations found |
| `node -e "require('lib/agents/adapters/dsh')"` | 0 | Adapter loads without error |
| `wc -l lib/agents/adapters/dsh.js` | n/a | 253 行（远低于 code-standards.md 500 行软上限） |
| `wc -l tests/agent/agent-adapter-dsh.test.js` | n/a | 240 行（10 用例 + helper） |

## Findings

### Passed

- `DshAdapter extends BaseAdapter` 并实现 5 方法契约的 MS-001 切片（discover + health + report 默认）。
- `discover()` 返回冻结的 metadata envelope，包含 `adapter_type / version / protocol / capabilities / schema / transport / cli / maturity / host / receipt_contract / capability_descriptor`，与 pi.js / claude-code.js / codex.js vendor 对称。
- `capability_descriptor` 通过 P-001 frozen vocabulary 校验（`validateCapabilityDescriptor` from `lib/runtime-adapters/capability-contract.js`），7 个 capability 全部使用 closed enum 的 `level` / `source`。
- `health()` 走 `which` (POSIX) / `where` (Windows) 解析，与 pi/codex/claude-code 完全一致；DSH_BIN env override 与 `CLAUDE_CODE_BIN` / `PI_BIN` / `CODEX_BIN` / `MINIMAX_BIN` 同位。
- `report()` 走 BaseAdapter 默认实现，缺失 journal 时返回标准 `not_found` 结构。
- 零 npm 依赖：源码仅 require `node:child_process` 与 `./base`（相对路径）。
- 安全边界：源码（除文档注释外）不含 `.dsh/sessions` 或 `session.jsonl` 引用；shadow usage 完全在 `scripts/dsh-usage-sync.js`。

### Failed

- 无。

### Waived

- 无。

## Follow-Up Fix Tasks

- [ ] MS-002：`lib/agents/adapters/dsh-bootstrap.js`（参照 `codey-pi-bootstrap.js`）— 显式加载入口。
- [ ] MS-002：`lib/agents/registry-adapter-types.js#VALID_ADAPTER_TYPES_EXT` 加入 `"dsh"`。
- [ ] MS-002：`lib/agents/adapters/index.js#_seed()` try/catch 注入 `DshAdapter`。
- [ ] MS-002：`lib/coordination/adapter-core.js` 注册 `HostAdapter.dsh`。
- [ ] MS-003：完整 `invoke()` + 6 类失败模式 + cancel + happy-path report。
- [ ] MS-004：双语模板 + `cortex-agent add dsh` + `docs/host-dsh-integration.md`。

## Decision

- Result: **PASS**（代码 + 测试 + 全套 baseline 全绿；架构 guard 无违规；零依赖硬约束保持）
- Next state: FIX_OR_ADVANCE → 提交 MS-001 工作并进入 MS-002（registry / `_seed()` / adapter-core.js 注册）
- Commit: `7d877a85f6bf18ccebbfc111485f0ebdd9172537`
- Receipts: `AR-commit-intent-20260819T091229Z.json` + `AR-commit-result-20260819T091235Z.json`
