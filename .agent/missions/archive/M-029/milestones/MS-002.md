# Milestone: MS-002 — DSH registry / `_seed()` / `adapter-core.js` 注册

> **Goal**: 把 DSH 加入 `VALID_ADAPTER_TYPES_EXT`，在 `adapters/index.js#_seed()` 用 try/catch 注入，新增 `dsh-bootstrap.js`，并在 `lib/coordination/adapter-core.js` 注册 `HostAdapter.dsh`；不动 `lib/agents/registry.js` 主体、不动 wakeup / handshake / structured-context。
>
> **Feature**: F-015
> **Validation Contract**: `../validation-contract.json#MS-002`
> **对应项目里程碑**: `agent-runtime-interoperability/index.md` M-015
> **前置**: MS-001 PASS

## Scope

- Feature(s): F-015
- Files/modules:
  - `lib/agents/registry-adapter-types.js` (修改 VALID_ADAPTER_TYPES_EXT)
  - `lib/agents/adapters/index.js` (修改 _seed() try/catch 注入)
  - `lib/agents/adapters/dsh-bootstrap.js` (new, ~70 行)
  - `lib/coordination/adapter-core.js` (新增 HostAdapter.dsh 注册)
- Validation contract: `../validation-contract.json#MS-002`

## Status

- State: PASS（EXECUTE_FEATURE 完成，VALIDATE_MILESTONE 由 mission-coordinator 接受）
- Started: 2026-08-19
- Completed: 2026-08-19
- Commit: `005b59e626b54b88bc466e34a5b08e1ceab892a5` — `feat(agents): 把 DSH 接入 registry / _seed() / coordination (MS-002 切片)`
- Validator: mission-coordinator

## Validation Results

| Assertion ID | Type | Blocking | Status | Evidence |
| :--- | :--- | :--- | :--- | :--- |
| VC-029-002-01 | test | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` — `adapters.list()` includes `'dsh'`；`adapters.getClass('dsh') === DshAdapter`；`adapters.get('dsh') instanceof DshAdapter` |
| VC-029-002-02 | api | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` — `VALID_ADAPTER_TYPES_EXT.includes('dsh')`；`isKnownAdapterType('dsh') === true`；`validateAdapterTypeExt('dsh')` 不抛；`minimax` 保留 |
| VC-029-002-03 | api | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` — `REGISTERED_ADAPTER_IDS.includes('dsh.local')` + `'dsh.dev'`；`createHostAdapter({adapterId: 'dsh.local', capabilities: []})` 通过；adapter 对象返回 `adapterId / schemaVersion / capabilities / handshakeOk=false / autoApprove=false / sideEffects=false` |
| VC-029-002-04 | security | true | **PASS** | `tests/agent/agent-adapter-dsh.test.js` — `VALID_ADAPTER_TYPES` 不含 `'dsh'` 也不含 `'minimax'`（M-002 frozen body 保持）；`_seed()` try/catch additive（`reset()` 重建后 `claude-code` / `codex` / `dsh` 全部回归） |

## Command Evidence

| Command | Exit Code | Result |
| :--- | :--- | :--- |
| `node --test tests/agent/agent-adapter-dsh.test.js` | 0 | 18/18 PASS（10 MS-001 + 8 MS-002） |
| `node --test tests/agent/*.test.js tests/coordination/*.test.js` | 0 | 1010/1010 PASS（既有 1002 + 8 新增；无回归） |
| `node .agent/skills/architecture-guard/scripts/index.js` | 0 | ✅ No architectural violations found |
| `git -c user.name=cortex-agent -c user.email=cortex-agent@local commit` | 0 | `[main 005b59e] feat(agents): 把 DSH 接入 registry / _seed() / coordination (MS-002 切片)` |

## Findings

### Passed

- `lib/agents/registry-adapter-types.js` 把 `"dsh"` 加入 `VALID_ADAPTER_TYPES_EXT`；`registry.js`（M-002 frozen）零修改。
- `lib/agents/adapters/index.js#_seed()` 追加 try/catch 注入 `DshAdapter`；`reset()` 重建后 `claude-code` / `codex` / `dsh` 全部回归。
- `lib/agents/adapters/dsh-bootstrap.js`（new, ~70 行）参照 `codey-pi-bootstrap.js` 提供 opt-in 加载入口；标记 `loaded=true` + `adapters=['dsh']`。
- `lib/coordination/adapter-core.js` 把 `"dsh.local"` 与 `"dsh.dev"` 加入 `REGISTERED_ADAPTER_IDS`；wakeup / handshake / structured-context 主流程零修改。
- `tests/agent/agent-adapter-dsh.test.js` 新增 8 个 MS-002 用例（VALID_ADAPTER_TYPES_EXT / list / get / bootstrap / REGISTERED_ADAPTER_IDS / createHostAdapter dsh.local / dsh.dev / registry.js frozen / _seed() reset）。

### Failed

- 无。

### Waived

- 无。

## Follow-Up Fix Tasks

- [ ] MS-003：完整 `invoke()` + 6 类失败模式 + cancel + happy-path report。
- [ ] MS-004：双语模板 + `cortex-agent add dsh` + `docs/host-dsh-integration.md` + `docs/platform-integration.md` 行 + `docs/architecture/adapter-authoring.md` §9。
- [ ] MS-005（可选）：DSH tool gate + context pilot。

## Decision

- Result: **PASS**
- Next state: FIX_OR_ADVANCE → 进入 MS-003（dispatch evidence sink + 6 类失败模式）
- Commit: `005b59e626b54b88bc466e34a5b08e1ceab892a5`
- Receipts: `AR-commit-intent-20260819T091833Z.json` + `AR-commit-result-20260819T091854Z.json`
