# Milestone: MS-005 — DSH tool gate + context pilot (optional)

> **Goal**: 如 DSH CLI 暴露 `beforeToolCall` / context hook：接入 P-001 gate 契约，并跑通 tool gate 与 context trajectory 试点；如不暴露，记录为 `unsupported`，readiness `warning|blocked`，发布为"first-class dispatch only"。
>
> **Feature**: F-018
> **Validation Contract**: `../validation-contract.json#MS-005`
> **对应项目里程碑**: `agent-runtime-interoperability/index.md` M-018
> **前置**: MS-004 PASS
> **可选**: DSH 真实 hook 能力尚未在仓库内固化证据；如不实施，发布为 first-class dispatch only

## Scope

- Feature(s): F-018
- Files/modules (条件性):
  - `lib/host-adapter/dsh-rpc-capability.js` (new, 仿 pi-rpc-capability.js)
  - `lib/runtime-adapters/tool-gate.js` (DSH 分支)
  - `docs/host-dsh-integration.md` §4 (limits 章节更新)
- Validation contract: `../validation-contract.json#MS-005`

## Status

- State: WAIVED-OPTIONAL（mission-coordinator 接受 waiver；P-006 §6 Phase 5 约定）
- Started: —（未启动实现）
- Completed: 2026-08-19（waiver 判定）
- Validator: mission-coordinator + 独立 validator

## Waiver 依据

1. P-006 §6 Phase 5 明确"**如不实施，发布为 first-class dispatch only**"；
2. P-006 §9 停止条件与 §8 风险表："DSH 提供 tool gate hook 但与 P-001 契约不符 / 未固化证据 → MS-005 推迟，记录为 `unsupported`，readiness `warning|blocked`"；
3. `lib/agents/adapters/dsh.js#discover().capability_descriptor` 已将 `tool.before.observe` / `tool.before.block` / `context.render.observe` 标记 `unsupported`（P-001 冻结词汇），与 waiver 一致；
4. 仓库内无 DSH 真实 hook 能力（`beforeToolCall` / `transformContext`）证据 —— 不得假设支持；
5. 发布形态 = **first-class dispatch only**（M-014～M-017 全部完成，MS-001～MS-004 PASS）。

## Scope

- Feature(s): F-018（条件性：DSH 真实 hook 能力证据出现后再评估）
- 若未来获得证据：单列 P-007 follow-up 提案，复用 `lib/host-adapter/pi-rpc-capability.js` 模式新增 `dsh-rpc-capability.js` + tool gate 分支。
- Validation contract: `../validation-contract.json#MS-005`（blocking: false 断言）

## Validation Results

| Assertion ID | Type | Blocking | Status | Evidence |
| :--- | :--- | :--- | :--- | :--- |
| VC-029-005-01 | doc | false | **WAIVED** | DSH 真实 `tool.before` hook 无证据；capability_descriptor 保持 `tool.before.observe/block = unsupported`（P-006 §6 Phase 5 + §9） |
| VC-029-005-02 | doc | false | **WAIVED** | 发布形态 = first-class dispatch only（P-006 §6 注）；waiver 理由见本文件 §Waiver 依据 |

## Command Evidence

| Command | Exit Code | Result |
| :--- | :--- | :--- |
| `node --test tests/agent/agent-adapter-dsh.test.js` | 0 | 31/31 PASS（capability_descriptor 断言 `tool.before.block` = `unsupported`） |
| `grep "tool.before" lib/agents/adapters/dsh.js` | 0 | `tool.before.observe` / `tool.before.block` = `unsupported`（与 waiver 一致） |

## Findings

### Passed

- first-class dispatch 形态完整交付（MS-001～MS-004），tool gate 保持显式 `unsupported`，readiness 语义正确（`warning|blocked` 而非静默假装支持）。

### Failed

- 无。

### Waived

- VC-029-005-01（tool gate 接入）— approver: mission-coordinator，理由: 无 DSH 真实 hook 证据，P-006 §6 Phase 5 明确允许 first-class dispatch only。
- VC-029-005-02（waiver 记录）— 本条即证据。

## Follow-Up Fix Tasks

- [ ] 获得 DSH 真实 hook 能力证据后，单列 P-007 follow-up 提案（tool gate + context pilot）。
- [ ] 全部 milestone 完成后 `/publish-docs --architecture` 同步 `docs/host-dsh-integration.md` 到公开 `docs/`。

## Decision

- Result: **WAIVED**（optional milestone；不阻塞 COMPLETE）
- Next state: COMPLETE 判定（MS-001～MS-004 PASS + MS-005 WAIVED）
